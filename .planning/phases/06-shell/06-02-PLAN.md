---
phase: 06-shell
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/apps/shell/src/styles/design-tokens.css
  - frontend/apps/shell/src/styles/global.css
  - frontend/apps/shell/src/components/layout/AppLayout.vue
  - frontend/apps/shell/src/components/layout/AppHeader.vue
  - frontend/apps/shell/src/components/layout/AppSidebar.vue
  - frontend/apps/shell/src/components/navigation/NavItem.vue
  - frontend/apps/shell/src/components/ui/GlassSurface.vue
  - frontend/apps/shell/src/components/ui/Avatar.vue
  - frontend/apps/shell/src/App.vue
autonomous: true
requirements:
  - FE-02
  - FE-09
must_haves:
  truths:
    - Shell renders with glassmorphism header and sidebar
    - Layout is responsive: desktop 1440px shows full sidebar, tablet 768px shows compact sidebar
    - All design tokens from DESIGN.md applied (colors, typography, spacing, shapes)
    - Navigation items show active state with visual indicator
  artifacts:
    - path: frontend/apps/shell/src/styles/design-tokens.css
      provides: CSS custom properties for all design tokens
      min_lines: 80
    - path: frontend/apps/shell/src/components/layout/AppLayout.vue
      provides: Main layout with header, sidebar, content area
      exports: ["default"]
    - path: frontend/apps/shell/src/components/layout/AppHeader.vue
      provides: Glassmorphism header with user avatar and actions
      exports: ["default"]
    - path: frontend/apps/shell/src/components/layout/AppSidebar.vue
      provides: Glassmorphism sidebar with navigation
      exports: ["default"]
  key_links:
    - from: frontend/apps/shell/src/App.vue
      to: frontend/apps/shell/src/components/layout/AppLayout.vue
      via: component import
      pattern: "import.*AppLayout"
    - from: frontend/apps/shell/src/components/layout/AppLayout.vue
      to: frontend/apps/shell/src/styles/design-tokens.css
      via: CSS imports
      pattern: "var\\(--"
user_setup: []
---

<objective>
Implement the Luminous Knowledge design system in the shell app, creating a responsive glassmorphism layout with header, sidebar, and main content area. Apply all design tokens from DESIGN.md including colors, typography, spacing, and shapes.

Purpose: Provide a premium, consistent visual experience across the knowledge management platform with Apple-inspired minimalism and glassmorphism.
Output: Design token CSS, layout components, and styled global shell.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@DESIGN.md
@frontend/apps/shell/src/App.vue

**Design System Tokens (from DESIGN.md):**
- Colors:
  - Primary: #0058bc (was #007AFF in ROADMAP, but DESIGN.md uses #0058bc)
  - Background: #f9f9ff
  - Surface variants: #f9f9ff, #f1f3fe, #ecedf9, #e6e8f3
  - On-surface: #181c23
  - On-surface-variant: #414755
- Typography: Inter font family
  - Display-lg: 48px, weight 700, line-height 1.1, letter-spacing -0.02em
  - Headline-md: 24px, weight 600, line-height 1.25, letter-spacing -0.01em
  - Body-base: 17px, weight 400, line-height 1.5, letter-spacing -0.01em
  - Body-sm: 14px, weight 400, line-height 1.4
  - Label-caps: 12px, weight 600, letter-spacing 0.05em
- Spacing:
  - Unit: 8px
  - Gutter: 24px
  - Margin-desktop: 40px
  - Margin-mobile: 16px
- Shapes:
  - Buttons: 10px radius
  - Cards: 12-16px radius
  - List items: 6-8px radius
- Glassmorphism:
  - Backdrop-filter: blur(20px) saturate(180%)
  - Background: rgba(255, 255, 255, 0.7)
  - Border: 1px solid #E5E5E7

**Existing Setup:** App.vue has basic sidebar layout but needs glassmorphism styling.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Design Tokens CSS</name>
  <files>frontend/apps/shell/src/styles/design-tokens.css, frontend/apps/shell/src/styles/global.css</files>
  <action>
    Create `frontend/apps/shell/src/styles/design-tokens.css` with CSS custom properties:
    - :root {
      /* Colors from DESIGN.md */
      --color-primary: #0058bc;
      --color-on-primary: #ffffff;
      --color-background: #f9f9ff;
      --color-on-background: #181c23;
      --color-surface: #f9f9ff;
      --color-surface-dim: #d8d9e5;
      --color-surface-container-low: #f1f3fe;
      --color-surface-container: #ecedf9;
      --color-surface-container-high: #e6e8f3;
      --color-on-surface: #181c23;
      --color-on-surface-variant: #414755;
      --color-outline: #717786;
      --color-outline-variant: #c1c6d7;
      --color-error: #ba1a1a;
      --color-on-error: #ffffff;
      
      /* Typography */
      --font-family: 'Inter', system-ui, -apple-system, sans-serif;
      --font-display-lg: 700 48px/1.1 var(--font-family);
      --font-headline-md: 600 24px/1.25 var(--font-family);
      --font-body-base: 400 17px/1.5 var(--font-family);
      --font-body-sm: 400 14px/1.4 var(--font-family);
      --font-label-caps: 600 12px/1.2 var(--font-family);
      --tracking-tight: -0.02em;
      --tracking-normal: -0.01em;
      --tracking-wide: 0.05em;
      
      /* Spacing */
      --spacing-unit: 8px;
      --spacing-xs: 4px;
      --spacing-sm: 8px;
      --spacing-md: 16px;
      --spacing-lg: 24px;
      --spacing-xl: 32px;
      --spacing-2xl: 48px;
      --gutter: 24px;
      --margin-desktop: 40px;
      --margin-mobile: 16px;
      --max-width: 1200px;
      
      /* Shapes */
      --radius-sm: 6px;
      --radius-md: 10px;
      --radius-lg: 12px;
      --radius-xl: 16px;
      --radius-full: 9999px;
      
      /* Glassmorphism */
      --glass-bg: rgba(255, 255, 255, 0.7);
      --glass-bg-dark: rgba(255, 255, 255, 0.85);
      --glass-border: 1px solid rgba(229, 229, 231, 0.8);
      --glass-blur: blur(20px) saturate(180%);
      
      /* Shadows */
      --shadow-sm: 0 1px 2px rgba(0, 0, 0, 0.05);
      --shadow-md: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
      --shadow-glow: 0 0 20px rgba(0, 88, 188, 0.15);
      
      /* Transitions */
      --transition-fast: 150ms ease;
      --transition-base: 200ms ease;
    }

    Create `frontend/apps/shell/src/styles/global.css`:
    - Import design-tokens.css
    - Import Google Fonts: Inter
    - Base styles: html, body { margin: 0; padding: 0; background: var(--color-background); color: var(--color-on-background); font: var(--font-body-base); }
    - Utility classes for typography, spacing, colors
    - Focus styles with primary color outline
    - Scrollbar styling
  </action>
  <verify>
    <automated>cd frontend/apps/shell && npx tsc --noEmit 2>/dev/null || echo "TypeScript check skipped (no TS files yet)"</automated>
  </verify>
  <done>Both CSS files exist with all design tokens defined, global styles applied</done>
</task>

<task type="auto">
  <name>Task 2: Glass Surface Base Component</name>
  <files>frontend/apps/shell/src/components/ui/GlassSurface.vue</files>
  <action>
    Create `frontend/apps/shell/src/components/ui/GlassSurface.vue`:
    - Props:
      - variant: 'header' | 'sidebar' | 'card' | 'popover' (default: 'card')
      - blur: number (default: 20)
      - opacity: number (default: 0.7)
      - border: boolean (default: true)
      - shadow: 'none' | 'sm' | 'md' | 'lg' (default: 'sm')
      - radius: 'sm' | 'md' | 'lg' | 'xl' | 'none' (default: 'lg')
      - padding: 'none' | 'sm' | 'md' | 'lg' | 'xl' (default: 'md')
    - Template: single div with glassmorphism styles
    - Styles:
      - backdrop-filter: blur({blur}px) saturate(180%)
      - background: rgba(255, 255, 255, {opacity})
      - border: var(--glass-border) if border true
      - border-radius based on radius prop
      - box-shadow based on shadow prop
      - padding based on padding prop
    - Use CSS custom properties from design-tokens
    - Export as default component
  </action>
  <verify>
    <automated>cd frontend/apps/shell && cat src/components/ui/GlassSurface.vue | grep -E "(variant|blur|opacity|backdrop-filter)" | wc -l</automated>
  </verify>
  <done>GlassSurface.vue exists with all props and glassmorphism styles</done>
</task>

<task type="auto">
  <name>Task 3: Navigation Components</name>
  <files>frontend/apps/shell/src/components/navigation/NavItem.vue</files>
  <action>
    Create `frontend/apps/shell/src/components/navigation/NavItem.vue`:
    - Props:
      - to: string (required) - router path
      - icon: string (optional) - icon name or component
      - label: string (required) - display text
      - active: boolean (default: false)
      - collapsed: boolean (default: false) - show only icon
    - Template:
      - router-link wrapper
      - Icon on left (16x16 or 20x20)
      - Label text
      - Active indicator: left border 3px primary color or background highlight
    - Styles:
      - Display: flex, align-items: center, gap: var(--spacing-md)
      - Padding: var(--spacing-sm) var(--spacing-md)
      - Border-radius: var(--radius-md)
      - Transition: all var(--transition-fast)
      - Hover: background: var(--color-surface-container-low)
      - Active: background: var(--color-surface-container), color: var(--color-primary), font-weight: 500
      - Active indicator: pseudo-element left border or background highlight
      - If collapsed: center icon, hide label, square aspect
    - Use design tokens exclusively
    - Export as default component
  </action>
  <verify>
    <automated>cd frontend/apps/shell && cat src/components/navigation/NavItem.vue | grep -E "(router-link|NavItem|active)" | head -5</automated>
  </verify>
  <done>NavItem.vue exists with router-link, icon, label, and active state styling</done>
</task>

<task type="auto">
  <name>Task 4: App Sidebar Component</name>
  <files>frontend/apps/shell/src/components/layout/AppSidebar.vue</files>
  <action>
    Create `frontend/apps/shell/src/components/layout/AppSidebar.vue`:
    - Props:
      - collapsed: boolean (default: false) - for tablet/mobile
      - user: User | null - from auth store
    - Template structure:
      - GlassSurface wrapper (variant: 'sidebar')
      - Logo section at top (Luminous Knowledge icon + text, hide text if collapsed)
      - Navigation section with NavItem components:
        - /search - Search
        - /domains - Domains
        - /ingestion - Ingestion
        - /admin - Admin (only if user has 'km-admin' role)
      - User section at bottom with avatar and logout
    - Styles:
      - Position: fixed left
      - Height: 100vh
      - Width: 240px (desktop), 72px (collapsed)
      - z-index: 50
      - Flex column, justify-content: space-between
      - Border-right: var(--glass-border)
      - Glassmorphism: blur(30px), bg rgba(255,255,255,0.75)
    - Use NavItem for each navigation link
    - Active route detection via vue-router useRoute
    - Export as default component
  </action>
  <verify>
    <automated>cd frontend/apps/shell && cat src/components/layout/AppSidebar.vue | grep -E "(GlassSurface|NavItem|router-link)" | wc -l</automated>
  </verify>
  <done>AppSidebar.vue exists with GlassSurface, NavItems, logo, and user section</done>
</task>

<task type="auto">
  <name>Task 5: App Header Component</name>
  <files>frontend/apps/shell/src/components/layout/AppHeader.vue, frontend/apps/shell/src/components/ui/Avatar.vue</files>
  <action>
    Create `frontend/apps/shell/src/components/ui/Avatar.vue`:
    - Props:
      - src: string | null - image URL
      - name: string - user name (fallback)
      - size: 'sm' | 'md' | 'lg' (default: 'md')
    - Template:
      - If src: img with border-radius: 50%
      - Else: initials circle (first letter of first two words)
    - Styles:
      - sm: 24px, md: 32px, lg: 40px
      - Background: var(--color-primary)
      - Color: white
      - Font: var(--font-label-caps)
      - Border-radius: var(--radius-full)

    Create `frontend/apps/shell/src/components/layout/AppHeader.vue`:
    - Props:
      - title: string - page title
      - user: User | null
    - Template:
      - GlassSurface wrapper (variant: 'header')
      - Left: h1 with page title (font: var(--font-headline-md))
      - Right: user info with Avatar + name + logout button
    - Styles:
      - Position: fixed top
      - Height: 64px
      - Left margin: matches sidebar width (240px or 72px)
      - Right: var(--margin-desktop)
      - z-index: 40
      - Glassmorphism: blur(20px), bg rgba(255,255,255,0.7)
      - Flex, justify-content: space-between, align-items: center
      - Border-bottom: var(--glass-border)
    - Export as default component
  </action>
  <verify>
    <automated>cd frontend/apps/shell && cat src/components/layout/AppHeader.vue | grep -E "(GlassSurface|Avatar|title)" | wc -l</automated>
  </verify>
  <done>AppHeader.vue and Avatar.vue exist with proper glassmorphism and user display</done>
</task>

<task type="auto">
  <name>Task 6: App Layout Component</name>
  <files>frontend/apps/shell/src/components/layout/AppLayout.vue</files>
  <action>
    Create `frontend/apps/shell/src/components/layout/AppLayout.vue`:
    - Props:
      - title: string - page title for header
    - Template structure:
      - div.layout-container
        - AppSidebar (fixed left)
        - div.main-content (pushed right by sidebar width)
          - AppHeader (fixed top within main)
          - main.content-area (scrollable, below header)
            - slot for page content
    - Script:
      - Import { ref, computed } from 'vue'
      - Import { useWindowSize } from '@vueuse/core' or custom composable
      - Import { useAuth } from '../../composables/useAuth'
      - Import AppSidebar from './AppSidebar.vue'
      - Import AppHeader from './AppHeader.vue'
      - Reactive sidebarCollapsed based on window width
      - Pass user and collapsed to sidebar
      - Pass title and user to header
    - Styles:
      - .layout-container: min-height: 100vh, background: var(--color-background)
      - .main-content: margin-left: 240px (desktop) or 72px (tablet/mobile)
      - .content-area: margin-top: 64px, padding: var(--spacing-lg), min-height: calc(100vh - 64px), overflow-y: auto
      - Responsive breakpoints:
        - Desktop (>=1440px): full sidebar 240px
        - Tablet (768px-1439px): collapsed sidebar 72px
        - Mobile (<768px): overlay sidebar with toggle button
    - Export as default component
  </action>
  <verify>
    <automated>cd frontend/apps/shell && cat src/components/layout/AppLayout.vue | grep -E "(AppSidebar|AppHeader|slot)" | wc -l</automated>
  </verify>
  <done>AppLayout.vue exists with sidebar, header, content slot, and responsive styles</done>
</task>

<task type="auto">
  <name>Task 7: Responsive Breakpoints and Mobile Support</name>
  <files>frontend/apps/shell/src/composables/useBreakpoint.ts, frontend/apps/shell/src/components/layout/AppSidebar.vue</files>
  <action>
    Create `frontend/apps/shell/src/composables/useBreakpoint.ts`:
    - Reactive composable for window size detection
    - Returns: { isMobile, isTablet, isDesktop, sidebarCollapsed }
    - Breakpoints:
      - mobile: < 768px
      - tablet: 768px - 1439px
      - desktop: >= 1440px
    - Use window.matchMedia or resize listener
    - Cleanup on unmount

    Update `AppSidebar.vue`:
    - Add mobile overlay behavior
    - Add toggle button for mobile (hamburger icon)
    - When mobile: position fixed, full overlay with backdrop blur
    - Close on outside click or link click
    - Transition animations for open/close

    Update `AppLayout.vue`:
    - Use useBreakpoint composable
    - Pass collapsed state to sidebar
    - Adjust main-content margin based on breakpoint
  </action>
  <verify>
    <automated>cd frontend/apps/shell && cat src/composables/useBreakpoint.ts | grep -E "(isMobile|isTablet|isDesktop)" | wc -l</automated>
  </verify>
  <done>useBreakpoint composable exists, sidebar has mobile overlay behavior</done>
</task>

<task type="auto">
  <name>Task 8: Integrate Layout into App.vue</name>
  <files>frontend/apps/shell/src/App.vue</files>
  <action>
    Update `frontend/apps/shell/src/App.vue`:
    - Replace entire content with:
    ```vue
    <template>
      <AppLayout v-if="isInitialized" :title="pageTitle">
        <router-view />
      </AppLayout>
      <div v-else class="loading-screen">
        <div class="spinner"></div>
        <p>Loading...</p>
      </div>
    </template>
    
    <script setup lang="ts">
    import { computed, watch } from 'vue'
    import { useRoute } from 'vue-router'
    import { useAuth } from './composables/useAuth'
    import AppLayout from './components/layout/AppLayout.vue'
    
    const route = useRoute()
    const { isAuthenticated, isLoading: authLoading, initialize } = useAuth()
    
    // Initialize auth on app mount
    initialize()
    
    const isInitialized = computed(() => !authLoading.value)
    
    const pageTitle = computed(() => {
      const titles: Record<string, string> = {
        '/search': 'Search Knowledge',
        '/domains': 'Domains',
        '/ingestion': 'Ingestion',
        '/admin': 'Administration'
      }
      return titles[route.path] || 'Knowledge Management'
    })
    </script>
    
    <style>
    @import './styles/global.css';
    
    .loading-screen {
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      min-height: 100vh;
      background: var(--color-background);
      color: var(--color-on-surface-variant);
    }
    
    .spinner {
      width: 40px;
      height: 40px;
      border: 3px solid var(--color-surface-container);
      border-top-color: var(--color-primary);
      border-radius: 50%;
      animation: spin 1s linear infinite;
      margin-bottom: var(--spacing-md);
    }
    
    @keyframes spin {
      to { transform: rotate(360deg); }
    }
    </style>
    ```
  </action>
  <verify>
    <automated>cd frontend/apps/shell && npx tsc --noEmit 2>/dev/null; npm run build 2>&1 | head -20</automated>
  </verify>
  <done>App.vue uses AppLayout with router-view, loading state, and global styles imported</done>
</task>

</tasks>

<verification>
1. CSS custom properties defined in design-tokens.css match DESIGN.md
2. Glassmorphism effect visible: backdrop-filter: blur(20px) saturate(180%)
3. Responsive breakpoints implemented: desktop (>=1440px), tablet (768px-1439px), mobile (<768px)
4. Sidebar width: 240px desktop, 72px tablet, overlay mobile
5. Header height: 64px, fixed position
6. Content area scrollable with proper margins
7. All components use CSS custom properties, no hardcoded values
8. Typography uses Inter font family
9. Primary color: #0058bc
10. Background: #f9f9ff
</verification>

<success_criteria>
1. `frontend/apps/shell/src/styles/design-tokens.css` exists with all CSS custom properties
2. `frontend/apps/shell/src/styles/global.css` exists with base styles and font import
3. `frontend/apps/shell/src/components/ui/GlassSurface.vue` exists with glassmorphism styles
4. `frontend/apps/shell/src/components/ui/Avatar.vue` exists with size variants
5. `frontend/apps/shell/src/components/navigation/NavItem.vue` exists with active states
6. `frontend/apps/shell/src/components/layout/AppSidebar.vue` exists with navigation items
7. `frontend/apps/shell/src/components/layout/AppHeader.vue` exists with user info
8. `frontend/apps/shell/src/components/layout/AppLayout.vue` exists with responsive layout
9. `frontend/apps/shell/src/composables/useBreakpoint.ts` exists for responsive detection
10. App.vue imports global.css and uses AppLayout
11. Build succeeds without errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-shell/06-02-SUMMARY.md`
</output>
