{
  "name": "knowledge-management-frontend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "npm run --prefix apps/shell dev",
    "build:remotes": "npm run --prefix apps/domains-ui build && npm run --prefix apps/search-ui build && npm run --prefix apps/ingestion-ui build && npm run --prefix apps/admin-ui build",
    "dev:all": "npm run build:remotes && concurrently --kill-others-on-fail -n domains,search,ingestion,admin,shell \"npm run --prefix apps/domains-ui preview\" \"npm run --prefix apps/search-ui preview\" \"npm run --prefix apps/ingestion-ui preview\" \"npm run --prefix apps/admin-ui preview\" \"npm run --prefix apps/shell dev\""
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}